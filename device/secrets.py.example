# PrestoDeck WiFi credentials -- TEMPLATE.
#
# Copy this file to ``secrets.py`` (same directory) and fill in your real
# network credentials. The real ``secrets.py`` is gitignored and must NEVER be
# committed. Do not put real credentials in this ``.example`` file.

WIFI_SSID = "your-network-name"
WIFI_PASSWORD = "your-network-password"
